import { styled } from '@styles/stitches.config'

export namespace ButtonLeadingStyles {
  export const Container = styled('View', {
    alignItems: 'center',
    justifyContent: 'center',

    marginLeft: -4,
  })
}
