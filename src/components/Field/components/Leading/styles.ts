import { styled } from '@styles/stitches.config'

export namespace FieldLeadingStyles {
  export const Container = styled('View', {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',

    width: 44,

    marginLeft: 4,
  })
}
