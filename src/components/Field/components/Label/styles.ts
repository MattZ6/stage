import { styled } from '@styles/stitches.config'

export namespace FieldLabelStyles {
  export const Text = styled('Text', {
    fontFamily: '$semiBold',
    fontSize: '$sm',
    lineHeight: '$sm',
    color: '$textLowContrast',

    marginBottom: '$sm',
  })
}
