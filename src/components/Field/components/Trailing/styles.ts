import { styled } from '@styles/stitches.config'

export namespace FieldTrailingStyles {
  export const Container = styled('View', {
    flexDirection: 'row',
    alignItems: 'center',

    width: 44,

    marginRight: 4,
  })
}
