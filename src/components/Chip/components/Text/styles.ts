import { styled } from '@styles/stitches.config'

export namespace ChipTextStyles {
  export const Text = styled('Text', {
    fontFamily: '$medium',
    fontSize: '$sm',
    lineHeight: '$sm',
    color: '$textHighContrast',
    textAlign: 'center',
  })
}
