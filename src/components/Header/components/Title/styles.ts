import { styled } from '@styles/stitches.config'

export namespace HeaderTitleStyles {
  export const Text = styled('Text', {
    fontFamily: '$semiBold',
    fontSize: '$md',
    lineHeight: '$md',
    color: '$textHighContrast',
    textAlign: 'center',
  })
}
