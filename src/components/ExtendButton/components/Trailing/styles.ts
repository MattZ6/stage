import { styled } from '@styles/stitches.config'

export namespace ExtendButtonTrailingStyles {
  export const Container = styled('View', {
    alignItems: 'center',
    justifyContent: 'center',

    marginRight: -4,
  })
}
